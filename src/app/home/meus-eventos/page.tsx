import { MyEventsList } from "@/components/dashboard/my-events/my-events-list";

export const metadata = {
  title: "Meus Eventos",
};

export default function MeusEventosPage() {
  return <MyEventsList />;
}
