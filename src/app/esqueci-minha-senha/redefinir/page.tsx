import { ResetPasswordForm } from "@/components/forgot-password";

export const metadata = {
  title: "Redefinir senha | Amparian",
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
